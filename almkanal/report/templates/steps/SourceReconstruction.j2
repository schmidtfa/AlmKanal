{# Optional refs:
{% set _ = ref('Glasser2016','Glasser, M. F., et al. (2016). A multi-modal parcellation of human cerebral cortex. *Nature*, 536, 171–178.') %}
#}

Sensor data were projected to source space using the spatial filters, and
{%- if s.settings.atlas %} parcellated with the {{ 'HCP-MMP1' if s.settings.atlas|lower == 'glasser' else s.settings.atlas }} atlas{%- endif -%}
{%- if s.settings.label_mode %} (label aggregation: {{ s.settings.label_mode }}){%- endif -%}.
{%- if s.settings.n_labels %} The parcellation comprised {{ s.settings.n_labels }} labels.{% endif -%}
