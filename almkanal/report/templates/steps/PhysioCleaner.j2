{# generic physiological artifact regression / cleaning #}
Physiological artifact cleaning was applied
{%- set extras = [] -%}
{%- for k, v in s.settings.items() if v is not none -%}
{%- set _ = extras.append(k ~ '=' ~ v) -%}
{%- endfor -%}
{%- if extras %} ({{ extras | join('; ') }}){% endif %}.
